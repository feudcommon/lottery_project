{
  "name": "scai-lucky-loop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "apk add --no-cache python3 make g++ && cd backend && npm install"
  }
}